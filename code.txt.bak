Inicio
  Se (Fechamento > Fechamento[1]) e (Minima < Minima[1]) e (Minima < Minima[2]) e (Fechamento > Abertura) então
    PaintBar(clAzul)
  Senão Se (Fechamento > Abertura) então
    PaintBar(clVerde)
  Senão Se (Fechamento < Abertura) então
    PaintBar(clVermelho)
  Senão Se (Fechamento = Abertura) então
    PaintBar(clVerdeOliva);
Fim;